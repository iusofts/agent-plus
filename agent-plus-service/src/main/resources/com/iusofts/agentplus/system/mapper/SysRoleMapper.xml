<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.iusofts.agentplus.system.dao.SysRoleMapper">

	<resultMap type="com.iusofts.agentplus.system.entity.SysRole" id="SysRoleResult">
		<id     property="roleId"             column="role_id"               />
		<result property="roleName"           column="role_name"             />
		<result property="roleKey"            column="role_key"              />
		<result property="roleSort"           column="role_sort"             />
		<result property="dataScope"          column="data_scope"            />
		<result property="permissions"        column="permissions"           />
		<result property="status"             column="status"                />
		<result property="createBy"           column="create_by"             />
		<result property="createTime"         column="create_time"           />
		<result property="updateBy"           column="update_by"             />
		<result property="updateTime"         column="update_time"           />
		<result property="remark"             column="remark"                />
	</resultMap>
	
	<sql id="selectRoleVo">
	    select distinct r.role_id, r.role_name, r.role_key, r.role_sort, r.data_scope, r.permissions,
            r.status, r.create_time, r.remark
        from sys_role r
	        left join sys_user_role ur on ur.role_id = r.role_id
	        left join sys_user u on u.user_id = ur.user_id
	        left join sys_dept d on u.dept_id = d.dept_id
    </sql>
    
    <select id="selectRoleList" parameterType="com.iusofts.agentplus.system.dto.SysRoleDto" resultMap="SysRoleResult">
		<include refid="selectRoleVo"/>
		where r.delete_flag = 0
		<if test="param.roleId != null and param.roleId != 0">
			AND r.role_id = #{param.roleId}
		</if>
		<if test="param.roleName != null and param.roleName != ''">
			AND r.role_name like concat('%', #{param.roleName}, '%')
		</if>
		<if test="param.status != null and param.status != ''">
			AND r.status = #{param.status}
		</if>
		<if test="param.roleKey != null and param.roleKey != ''">
			AND r.role_key like concat('%', #{param.roleKey}, '%')
		</if>
		<if test="param.params.beginTime != null and param.params.beginTime != ''"><!-- 开始时间检索 -->
			and date_format(r.create_time,'%Y%m%d') &gt;= date_format(#{param.params.beginTime},'%Y%m%d')
		</if>
		<if test="param.params.endTime != null and param.params.endTime != ''"><!-- 结束时间检索 -->
			and date_format(r.create_time,'%Y%m%d') &lt;= date_format(#{param.params.endTime},'%Y%m%d')
		</if>
		order by r.role_sort
	</select>
    
	<select id="selectRolePermissionByUserId" parameterType="java.lang.Long" resultMap="SysRoleResult">
		<include refid="selectRoleVo"/>
		WHERE r.delete_flag = 0 and ur.user_id = #{userId}
	</select>
	
	<select id="selectRoleAll" resultMap="SysRoleResult">
		<include refid="selectRoleVo"/>
		WHERE r.delete_flag = 0
	</select>
	
	<select id="selectRoleListByUserId" parameterType="java.lang.Long" resultType="java.lang.Long">
		select r.role_id
        from sys_role r
	        left join sys_user_role ur on ur.role_id = r.role_id
	        left join sys_user u on u.user_id = ur.user_id
	    where u.user_id = #{userId} and r.delete_flag = 0
	</select>
	
	<select id="selectRoleById" parameterType="java.lang.Long" resultMap="SysRoleResult">
		<include refid="selectRoleVo"/>
		where r.role_id = #{roleId}
	</select>
	
	<select id="selectRolesByUserName" parameterType="java.lang.String" resultMap="SysRoleResult">
		<include refid="selectRoleVo"/>
		WHERE r.delete_flag = 0 and u.username = #{userName} and r.delete_flag = 0
	</select>
	
	<select id="checkRoleNameUnique" parameterType="java.lang.String" resultMap="SysRoleResult">
		<include refid="selectRoleVo"/>
		 where r.role_name=#{roleName} and r.delete_flag = 0 limit 1
	</select>
	
	<select id="checkRoleKeyUnique" parameterType="java.lang.String" resultMap="SysRoleResult">
		<include refid="selectRoleVo"/>
		 where r.role_key=#{roleKey} and r.delete_flag = 0 limit 1
	</select>
	
 	<insert id="insertRole" parameterType="com.iusofts.agentplus.system.entity.SysRole" useGeneratedKeys="true" keyProperty="roleId">
 		insert into sys_role(
 			<if test="roleId != null and roleId != 0">role_id,</if>
 			<if test="roleName != null and roleName != ''">role_name,</if>
 			<if test="roleKey != null and roleKey != ''">role_key,</if>
 			<if test="roleSort != null">role_sort,</if>
 			<if test="dataScope != null">data_scope,</if>
 			<if test="permissions != null and permissions != ''">permissions,</if>
 			<if test="status != null and status != ''">status,</if>
 			<if test="remark != null and remark != ''">remark,</if>
 			<if test="createBy != null and createBy != ''">create_by,</if>
 			create_time
 		)values(
 			<if test="roleId != null and roleId != 0">#{roleId},</if>
 			<if test="roleName != null and roleName != ''">#{roleName},</if>
 			<if test="roleKey != null and roleKey != ''">#{roleKey},</if>
 			<if test="roleSort != null">#{roleSort},</if>
 			<if test="dataScope != null">#{dataScope},</if>
 			<if test="permissions != null and permissions != ''">#{permissions},</if>
 			<if test="status != null and status != ''">#{status},</if>
 			<if test="remark != null and remark != ''">#{remark},</if>
 			<if test="createBy != null and createBy != ''">#{createBy},</if>
 			sysdate()
 		)
	</insert>
	
	<update id="updateRole" parameterType="com.iusofts.agentplus.system.entity.SysRole">
 		update sys_role
 		<set>
 			<if test="roleName != null and roleName != ''">role_name = #{roleName},</if>
 			<if test="roleKey != null and roleKey != ''">role_key = #{roleKey},</if>
 			<if test="roleSort != null">role_sort = #{roleSort},</if>
 			<if test="dataScope != null">data_scope = #{dataScope},</if>
 			<if test="permissions != null and permissions != ''">permissions = #{permissions},</if>
 			<if test="status != null and status != ''">status = #{status},</if>
 			<if test="remark != null">remark = #{remark},</if>
 			<if test="updateBy != null and updateBy != ''">update_by = #{updateBy},</if>
 			update_time = sysdate()
 		</set>
 		where role_id = #{roleId}
	</update>
	
	<delete id="deleteRoleById" parameterType="java.lang.Long">
 		update sys_role set delete_flag = 1 where role_id = #{roleId}
 	</delete>
 	
 	<delete id="deleteRoleByIds" parameterType="java.lang.Long">
 	    update sys_role set delete_flag = 1 where role_id in
 		<foreach collection="array" item="roleId" open="(" separator="," close=")">
 			#{roleId}
        </foreach> 
 	</delete>
 	
</mapper> 