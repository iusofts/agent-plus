<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.iusofts.system.dao.SysRoleMenuMapper">

	<resultMap type="com.iusofts.system.dto.SysRoleMenuDto" id="SysRoleMenuResult">
		<result property="roleId"     column="role_id"      />
		<result property="menuId"     column="menu_id"      />
	</resultMap>
	
	<select id="checkMenuExistRole" resultType="java.lang.Integer">
	    select count(1) from sys_role_menu where menu_id = #{menuId} and delete_flag = 0
	</select>

	<delete id="deleteRoleMenuByRoleId" parameterType="java.lang.Long">
		delete from sys_role_menu where role_id=#{roleId}
	</delete>
	
	<delete id="deleteRoleMenu" parameterType="java.lang.Long">
 		delete from sys_role_menu where role_id in
 		<foreach collection="array" item="roleId" open="(" separator="," close=")">
 			#{roleId}
        </foreach> 
 	</delete>
 	
	<insert id="batchRoleMenu">
		insert into sys_role_menu(role_id, menu_id) values
		<foreach item="item" index="index" collection="list" separator=",">
			(#{item.roleId},#{item.menuId})
		</foreach>
	</insert>

	<select id="selectMenuIdsByRoleId" parameterType="java.lang.Long" resultType="java.lang.Long">
		select menu_id from sys_role_menu where role_id = #{roleId} and delete_flag = 0
	</select>

</mapper> 